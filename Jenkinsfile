pipeline {
  agent any
  stages {
    stage('fetch') {
      steps {
        sh 'echo "fetch"'
      }
    }
    stage('build') {
      steps {
        sh 'echo "install and build"'
      }
    }
    stage('deploy') {
      steps {
        sh 'echo "deploy"'
      }
    }
  }
}